{
  "scripts": {
    "start": "npm run build:all && cd backend && npm run start:prod",
    "build:all": "cd frontend && npm install && npm run build && mkdir -p ../backend/client && cp -r dist/* ../backend/client/ && cd ../backend && npm install && npm run build"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}